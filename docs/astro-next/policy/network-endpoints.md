<!-- Generated by tools/policy/manifest.py — do not edit by hand.
     Regenerate with: tools/policy/manifest.py --write
     Judgement lives in `endpoints.json`; the host set is seeded from
     tools/baseline/inventory_endpoints.py and joined strictly. -->

# Astro Next network endpoints

Every host committed text references, with its disposition. The host set is derived on every run by `tools/policy/endpoint_seed.py`, which imports issue #6's `tools/baseline/inventory_endpoints.py` and adds a scan of `webui/` that generator does not perform. Nothing here is hand-copied.

**This is not a measured trace.** It is what the source says. A real network baseline needs a running browser behind a recorder; `tools/baseline/capture-network.sh` is that harness and it refuses to emit a trace it did not record. Runtime capture is blocked because no Astro binary exists yet — baseline finding 1.

### The decision

| State | Hosts |
|---|---|
| `KEEP` | 16 |
| `REPLACE` | 14 |
| `DISABLE_BUILD` | 46 |
| `DISABLE_RUNTIME` | 0 |
| `DORMANT` | 25 |
| `INVESTIGATE` | 21 |
| **total** | **122** |

### What the current build does — a separate axis

This is what makes `DISABLE_BUILD` and `DISABLE_RUNTIME` falsifiable rather than rhetorical. `REMOVED_BY_PATCH` means the literal is absent from the tree and a string scan of the artefact can prove it. `RUNTIME_BLOCKED` means the literal is **present**, the calling code is compiled and still reached, and only the net layer refuses — for these hosts the inherited iridium patch rewrites the URL to `trk:` and `block-trk-and-subdomains.patch` makes `trk:` unresolvable. The two look identical in a feature list and are entirely different in a threat model.

| Observed | Hosts |
|---|---|
| `REMOVED_BY_PATCH` | 32 |
| `NOT_BUILT` | 30 |
| `NOT_AN_ENDPOINT` | 23 |
| `RUNTIME_BLOCKED` | 20 |
| `PRESENT` | 14 |
| `NOT_MEASURED` | 3 |

### Contact

| Contact | Hosts |
|---|---|
| `never` | 85 |
| `user-initiated` | 25 |
| `automatic` | 6 |
| `unknown` | 6 |

### Why each host appears in committed text

| `reference_kind` | Hosts |
|---|---|
| `deleted-by-patch` | 38 |
| `documentation` | 21 |
| `blocklist-literal` | 20 |
| `default-content` | 18 |
| `request-literal` | 14 |
| `help-link` | 8 |
| `not-a-host` | 2 |
| `csp-allowlist` | 1 |

## Contacted automatically (6)

| Host | Observed | State | Kind | Feature | Owner | Trigger | Rationale |
|---|---|---|---|---|---|---|---|
| `api.oxy.so` | NOT_BUILT | **REPLACE** | request-literal | `oxy-identity` | #16 | an Oxy session cookie appears on auth.oxy.so | Oxy platform API. Called after sign-in to exchange the session for tokens and fetch the profile (oxy_cookie_signin_observer.cc:39-42, 63-65). REPLACE for the same reason as auth.oxy.so: #16 rebuilds the credential exchange. |
| `easylist-downloads.adblockplus.org` | NOT_BUILT | **REPLACE** | request-literal | `adblock-list-updates` | #19 | filter-list updater schedule | Filter-list source in the shipped catalog (src/chrome/browser/oxy/adblock/astro_adblock_filter_list_catalog.cc). REPLACE rather than KEEP: #19 requires signed, integrity-checked, atomic and recoverable list updates, and a plain HTTPS fetch of a third-party text file provides none of those. The list CONTENT survives; the transport and the trust root do not. |
| `easylist.to` | NOT_BUILT | **REPLACE** | request-literal | `adblock-list-updates` | #19 | filter-list updater schedule | Filter-list source in the shipped catalog (src/chrome/browser/oxy/adblock/astro_adblock_filter_list_catalog.cc). REPLACE rather than KEEP: #19 requires signed, integrity-checked, atomic and recoverable list updates, and a plain HTTPS fetch of a third-party text file provides none of those. The list CONTENT survives; the transport and the trust root do not. |
| `fonts.googleapis.com` | NOT_BUILT | **REPLACE** | request-literal | `webui-fonts` | #14 | any Astro WebUI page loads | A de-Googled browser whose New Tab Page contacts Google every time a tab opens. The reference is not incidental: astro_ntp_ui.cc:94 (style-src) and webui/{ntp,settings,alia}/src/index.html — the C++ CSP was widened specifically to permit it, and the HTML carries a preconnect plus a stylesheet link. REPLACE: the Inter typeface survives, packaged into browser resources; the remote provider does not. The epic forbids a privileged WebUI loading remote fonts, so this is not a preference. |
| `fonts.gstatic.com` | NOT_BUILT | **REPLACE** | request-literal | `webui-fonts` | #14 | any Astro WebUI page loads | A de-Googled browser whose New Tab Page contacts Google every time a tab opens. The reference is not incidental: astro_ntp_ui.cc:97 (font-src) and webui/{ntp,settings,alia}/src/index.html — the C++ CSP was widened specifically to permit it, and the HTML carries a preconnect plus a stylesheet link. REPLACE: the Inter typeface survives, packaged into browser resources; the remote provider does not. The epic forbids a privileged WebUI loading remote fonts, so this is not a preference. |
| `pgl.yoyo.org` | NOT_BUILT | **REPLACE** | request-literal | `adblock-list-updates` | #19 | filter-list updater schedule | Filter-list source in the shipped catalog (src/chrome/browser/oxy/adblock/astro_adblock_filter_list_catalog.cc). REPLACE rather than KEEP: #19 requires signed, integrity-checked, atomic and recoverable list updates, and a plain HTTPS fetch of a third-party text file provides none of those. The list CONTENT survives; the transport and the trust root do not. |

## Contacted only on a user action (25)

| Host | Observed | State | Kind | Feature | Owner | Trigger | Rationale |
|---|---|---|---|---|---|---|---|
| `api.alia.onl` | NOT_BUILT | **REPLACE** | request-literal | `alia` | #17 | user sends a message in the Alia side panel | The host the Alia panel actually calls (webui/alia/src/alia-panel.ts:31). REPLACE: #17 rebuilds Alia as a trusted shell with isolated content and explicit context consent, which changes what is sent here and from which process. |
| `astrobrowser.org` | PRESENT | **INVESTIGATE** | help-link | `product-help-links` | #9 | user clicks the link in settings | Linked from the Astro settings page (webui/settings/src/sections.ts:450). Every other Astro-owned host in this manifest is under oxy.so. |
| `auth.oxy.so` | NOT_BUILT | **REPLACE** | request-literal | `oxy-identity` | #16 | user starts sign-in from the profile menu | Oxy Identity's authorisation host. REPLACE, not KEEP: the current flow returns access_token and refresh_token as URL query parameters (src/chrome/browser/oxy/oxy_auth_callback_handler.cc:31-32), which the epic forbids outright. #16 rebuilds it on Authorization Code + PKCE, which changes the request shape at this host. |
| `chromium.googlesource.com` | PRESENT | **REPLACE** | help-link | `first-run-page` | #24 | user clicks a link on the first-run page | the Chromium source host, linked from the same first-run page and from Astro's Linux package metadata. The page itself is local (`chrome://ungoogled-first-run`), so first run makes no request; these hosts are links inside it. REPLACE: Astro must not open a page branded for another project on first run, and #24 owns first run. |
| `developer.chrome.com` | PRESENT | **REPLACE** | help-link | `product-help-links` | #9 | user clicks a link | Chrome developer documentation, linked from settings strings. REPLACE for the same reason as support.google.com: help destinations are Astro product constants. |
| `developer.mozilla.org` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `developers.google.com` | PRESENT | **REPLACE** | help-link | `product-help-links` | #9 | user clicks a link | Google developer documentation, linked from settings strings. REPLACE for the same reason as support.google.com: help destinations are Astro product constants. |
| `duckduckgo.com` | NOT_BUILT | **KEEP** | default-content | `default-search-engine` | #10 | user submits a search | Astro's default search engine (patches/astro/004-default-search-duckduckgo.patch) and the NTP search form's action (webui/ntp/src/index.html:375). Contacted when the user searches. |
| `github.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the tile | GitHub — a prepopulated New Tab Page tile added by patches/astro/019-ntp-default-sites.patch. Shipped data, not a request: the browser contacts it only when the user clicks the tile. Astro's tile set replaced Chromium's Google-owned defaults; the removed ones are in this manifest as `deleted-by-patch`. |
| `news.ycombinator.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `oxy.so` | PRESENT | **KEEP** | help-link | `product-help-links` | #9 | user clicks a product link | Astro's own product and package-metadata host (patches/astro/003-branding-linux-package.patch, webui/whats-new/src/index.html). User-initiated navigation only. |
| `reddit.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `search.brave.com` | NOT_BUILT | **KEEP** | default-content | `default-search-engine` | #10 | user submits a search with this engine selected | Brave Search, offered as a user-selectable search engine by the Astro New Tab Page (webui/ntp/src/main.ts). Reached only if the user picks it. Chromium's own Google search integrations are a separate decision — see the `google-search-default` feature, which is not KEEP. |
| `stackoverflow.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `support.9oo91e.qjz9zk` | PRESENT | **REPLACE** | help-link | `product-help-links` | #9 | user clicks 'learn more' on a redirect-loop error page | THE ONE SUBSTITUTED HOST THAT REACHES SHIPPED UI. patches/astro/038-dino-game-astro-scheme.patch rewrites kRedirectLoopLearnMoreUrl in components/error_page/common/localized_error.cc to `https://support.9oo91e.qjz9zk/chrome?p=rl_error` — the 'learn more' link on the ERR_TOO_MANY_REDIRECTS interstitial. That patch APPLIES, so today's build ships a link to a host that does not resolve and that block-trk-and-subdomains.patch blocks anyway. The other eleven substituted-shape hosts in this manifest sit in translator notes, `<ex>` placeholders and C++ comments, and are `documentation`. REPLACE: the link survives, pointing at an Astro destination. |
| `support.google.com` | PRESENT | **REPLACE** | help-link | `product-help-links` | #9 | user clicks a help link | Chromium's help-centre host, reached from settings and error pages. REPLACE: Astro's help destinations are product constants #9 generates from one source, not Google's. |
| `ungoogled-software.github.io` | PRESENT | **REPLACE** | help-link | `first-run-page` | #24 | user clicks a link on the first-run page | the ungoogled-chromium wiki, linked from the `chrome://ungoogled-first-run` page that patches/ungoogled/extra/ungoogled-chromium/first-run-page.patch adds and opens on first run. The page itself is local (`chrome://ungoogled-first-run`), so first run makes no request; these hosts are links inside it. REPLACE: Astro must not open a page branded for another project on first run, and #24 owns first run. |
| `wikipedia.org` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `www.bing.com` | NOT_BUILT | **KEEP** | default-content | `default-search-engine` | #10 | user submits a search with this engine selected | Bing, offered as a user-selectable search engine by the Astro New Tab Page (webui/ntp/src/main.ts). Reached only if the user picks it. Chromium's own Google search integrations are a separate decision — see the `google-search-default` feature, which is not KEEP. |
| `www.google.com` | NOT_BUILT | **KEEP** | default-content | `default-search-engine` | #10 | user submits a search with this engine selected | Google, offered as a user-selectable search engine by the Astro New Tab Page (webui/ntp/src/main.ts). Reached only if the user picks it. Chromium's own Google search integrations are a separate decision — see the `google-search-default` feature, which is not KEEP. |
| `www.reddit.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the tile | Reddit — a prepopulated New Tab Page tile added by patches/astro/019-ntp-default-sites.patch. Shipped data, not a request: the browser contacts it only when the user clicks the tile. Astro's tile set replaced Chromium's Google-owned defaults; the removed ones are in this manifest as `deleted-by-patch`. |
| `www.startpage.com` | NOT_BUILT | **KEEP** | default-content | `default-search-engine` | #10 | user submits a search with this engine selected | Startpage, offered as a user-selectable search engine by the Astro New Tab Page (webui/ntp/src/main.ts). Reached only if the user picks it. Chromium's own Google search integrations are a separate decision — see the `google-search-default` feature, which is not KEEP. |
| `www.wikipedia.org` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the tile | Wikipedia — a prepopulated New Tab Page tile added by patches/astro/019-ntp-default-sites.patch. Shipped data, not a request: the browser contacts it only when the user clicks the tile. Astro's tile set replaced Chromium's Google-owned defaults; the removed ones are in this manifest as `deleted-by-patch`. |
| `x.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |
| `youtube.com` | NOT_BUILT | **KEEP** | default-content | `ntp-tiles` | #22 | user clicks the link | A default quick link in the Astro New Tab Page (webui/ntp/src/main.ts). Shipped data; contacted only when the user clicks it. |

## Contact behaviour not established (6)

| Host | Observed | State | Kind | Feature | Owner | Trigger | Rationale |
|---|---|---|---|---|---|---|---|
| `cloudflare-dns.com` | NOT_BUILT | **INVESTIGATE** | default-content | `dns-over-https` | #10 → **#20** | every DNS resolution while DoH is active | DoH template offered by Astro settings — Cloudflare, and the DEFAULT (sections.ts:192). The settings handler maps this to the real Chromium prefs `dns_over_https.mode` and `dns_over_https.templates` (src/chrome/browser/oxy/webui/astro_settings_handler.cc:64-65), so a shipped default sends every DNS query the browser makes to one third party. |
| `connectivitycheck.gstatic.com` | NOT_MEASURED | **INVESTIGATE** | request-literal | `captive-portal-detection` | #10 | not-captured | The captive-portal / network-connectivity probe target. Chromium's probe is a `generate_204` request; 4 files in chromium/src carry a `gstatic.com/generate_204` literal at the locked commit. |
| `dns.nextdns.io` | NOT_BUILT | **INVESTIGATE** | default-content | `dns-over-https` | #10 → **#20** | every DNS resolution while DoH is active | DoH template offered by Astro settings — NextDNS, user-selectable. The settings handler maps this to the real Chromium prefs `dns_over_https.mode` and `dns_over_https.templates` (src/chrome/browser/oxy/webui/astro_settings_handler.cc:64-65), so a shipped default sends every DNS query the browser makes to one third party. |
| `dns.quad9.net` | NOT_BUILT | **INVESTIGATE** | default-content | `dns-over-https` | #10 → **#20** | every DNS resolution while DoH is active | DoH template offered by Astro settings — Quad9, user-selectable. The settings handler maps this to the real Chromium prefs `dns_over_https.mode` and `dns_over_https.templates` (src/chrome/browser/oxy/webui/astro_settings_handler.cc:64-65), so a shipped default sends every DNS query the browser makes to one third party. |
| `optimizationguide-pa.googleapis.com` | NOT_MEASURED | **INVESTIGATE** | request-literal | `on-device-models` | #8 | not-captured | Chromium's optimization-guide/on-device-model host. 1 file in chromium/src references it at the locked commit. |
| `update.googleapis.com` | NOT_MEASURED | **INVESTIGATE** | request-literal | `component-updater` | #20 → **#20** | not-captured | Chromium's component and extension update host. 2 files in chromium/src reference it at the locked commit ae03f7fb2c. |

## Never contacted (85)

| Host | Observed | State | Kind | Feature | Owner | Trigger | Rationale |
|---|---|---|---|---|---|---|---|
| `%s%s` | NOT_AN_ENDPOINT | **DORMANT** | not-a-host | `rlz` | #8 | none | A printf format string the URL scan matched, from patches/ungoogled/core/ungoogled-chromium/disable-untraceable-urls.patch, which replaces RLZ's financial server with `about:blank`. Kept in the manifest because the seed produces it: silently filtering the seed would make the join lie about what the inventory contains. |
| `%s:%d` | NOT_AN_ENDPOINT | **DORMANT** | not-a-host | `trk-scheme-blocking` | #8 | none | A printf format string from the iridium trk-prefix patch. Same reasoning as the RLZ one. |
| `9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `a.espncdn.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `accounts.google.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `google-account-gaia` | #8 | the owning feature's own trigger, if it runs | GAIA sign-in. ungoogled's disable-gaia.patch removes the account infrastructure. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `android.clients.google.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `gcm-push` | #8 | the owning feature's own trigger, if it runs | Android device/GCM registration. Not a desktop endpoint; Android is scoped separately. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `api.alia.oxy.so` | NOT_BUILT | **INVESTIGATE** | csp-allowlist | `alia` | #17 | — | The only host astro://alia's connect-src permits (src/chrome/browser/oxy/webui/astro_alia_ui.cc:95), and nothing calls it. The panel calls api.alia.onl, which that CSP blocks. One of the two is wrong and the manifest will not guess which. |
| `assets-cdn.github.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `beacons.gcp.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons2.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons3.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons4.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons5.gvt2.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `beacons5.gvt3.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `domain-reliability` | #8 | none | Deleted from the Chromium source by an inherited ungoogled patch; the host appears only on removed lines, so the patched tree does not contain it. The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `bugs.ch40m1um.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `bugs.chromium.org` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | Chromium issue-tracker links in comments. |
| `cdn2.iconfinder.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `chrome.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `web-store-and-update-urls` | #10 → **#20** | the owning feature's own trigger, if it runs | Legacy Web Store host. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `chromewebstore.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `web-store-and-update-urls` | #10 → **#20** | the owning feature's own trigger, if it runs | The Chrome Web Store. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `chromewebstore.googleapis.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `web-store-and-update-urls` | #10 → **#20** | the owning feature's own trigger, if it runs | The Web Store's API. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `chromium-review.googlesource.com` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A code-review link in a comment. |
| `clients2.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `component-updater` | #10 → **#20** | the owning feature's own trigger, if it runs | Omaha/component-update and crash endpoints. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `clientservices.googleapis.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `variations-field-trials` | #8 | the owning feature's own trigger, if it runs | The variations/field-trial seed server. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `codereview.chromium.org` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `crashpad.chromium.org` | NOT_AN_ENDPOINT | **DISABLE_BUILD** | documentation | `astro-crash-reporting` | #10 | none | A documentation URL in ungoogled's disable-crash-reporter.patch, not an upload target. The crash-reporting capability itself is a separate feature entry. |
| `crbug.com` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | Chromium bug links in patch comments. |
| `developer.ch40me.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `dns.google` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `dns-over-https` | #10 | none | Google Public DNS, deleted from the DoH provider list by patches/ungoogled/core/ungoogled-chromium/doh-changes.patch The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `dns64.dns.google` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `dns-over-https` | #10 | none | Google Public DNS64, deleted by the same patch The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `edition.cnn.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `en.m.wikipedia.org` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `footprints-pa.googleapis.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `google-history-footprints` | #8 | the owning feature's own trigger, if it runs | Web & App Activity ('footprints'). The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `freenode.net` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `goo.gle` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `goo.gle.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `google.com` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `history.google.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `google-history-footprints` | #8 | the owning feature's own trigger, if it runs | Google account history. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `i.cdn.cnn.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `images-na.ssl-images-amazon.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `images.unsplash.com` | NOT_BUILT | **DORMANT** | request-literal | `astro-newtab` | #22 | user selects the Unsplash wallpaper option | New Tab Page wallpaper source (webui/ntp/src/main.ts:345,352). Unreachable under the WebUI origin: astro_ntp_ui.cc:99-101 sets `img-src 'self' data:`, which admits neither host. Same shape as the weather widget — an arrangement, not a decision. |
| `ir.ebaystatic.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `localization.9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `localization.google.com` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `localizer.9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `localizer.google.com` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `m.ebay.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `m.facebook.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `m.youtube.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `mikewest.org` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A specification link in an inherited patch comment. |
| `pay.9oo91e.qjz9zk.` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `pay.google.com.` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `redirector.gvt1.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `component-updater` | #10 → **#20** | the owning feature's own trigger, if it runs | The CRX/component download redirector. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `remotedesktop.google.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `remoting` | #8 | the owning feature's own trigger, if it runs | Chrome Remote Desktop. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `s.yimg.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `safebrowsing.9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `safebrowsing.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `safe-browsing` | #20 → **#20** | the owning feature's own trigger, if it runs | The Safe Browsing service. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `safebrowsingohttpgateway.googleapis.com` | REMOVED_BY_PATCH | **INVESTIGATE** | deleted-by-patch | `safe-browsing` | #20 → **#20** | none | Deleted by patches/ungoogled/core/ungoogled-chromium/fix-building-without-safebrowsing.patch. |
| `safesearch.googleapis.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `safesearch` | #8 | the owning feature's own trigger, if it runs | SafeSearch enforcement. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `sb-ssl.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `safe-browsing` | #20 → **#20** | the owning feature's own trigger, if it runs | A Safe Browsing endpoint. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `source.chromium.org` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `disabled-ui-entries` | #8 | none | a credits link deleted by patches/ungoogled/extra/ungoogled-chromium/add-credits.patch The mechanism today is a patch, which #8 must re-express as build configuration or owned data — DISABLE_BUILD describes the intended end state, not the current one. |
| `source.unsplash.com` | NOT_BUILT | **DORMANT** | request-literal | `astro-newtab` | #22 | user selects the Unsplash wallpaper option | New Tab Page wallpaper source (webui/ntp/src/main.ts:345,352). Unreachable under the WebUI origin: astro_ntp_ui.cc:99-101 sets `img-src 'self' data:`, which admits neither host. Same shape as the weather widget — an arrangement, not a decision. |
| `ssl.gstatic.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `google-static-assets` | #10 | the owning feature's own trigger, if it runs | A Google static-asset host. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `static.xx.fbcdn.net` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `tools.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `component-updater` | #10 → **#20** | the owning feature's own trigger, if it runs | The update ping host; also targeted by block-requests.patch. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `translate.googleapis.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `translation` | #10 | the owning feature's own trigger, if it runs | The translation backend. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `transparencyreport.google.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `safe-browsing` | #20 → **#20** | the owning feature's own trigger, if it runs | Referenced by safe_browsing-disable-incident-reporting.patch. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `vasilvv.github.io` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A draft-specification link in an inherited patch comment. |
| `wallet.9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `wallet.9oo91e.qjz9zk.` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `wallet.google.com` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `wallet.google.com.` | PRESENT | **DISABLE_BUILD** | deleted-by-patch | `product-help-links` | #9 | none | A Google host in Chromium's string resources, replaced by an Astro branding patch (009/013/034/001). Appears only on removed lines. Most of these are translator `desc=` notes and `<ex>` placeholders that are never shipped as URLs; they are recorded because the same patches also rewrite real links, and only the diff side tells them apart. |
| `wttr.in` | NOT_BUILT | **DORMANT** | request-literal | `astro-newtab` | #22 | New Tab Page render, when not under a WebUI origin | The New Tab Page's weather widget (webui/ntp/src/main.ts:430). Unreachable in the shipped context by TWO independent mechanisms, both named: the page's own isWebUIContext() guard returns before the fetch (main.ts:424-427), and astro_ntp_ui.cc sets no connect-src, so the WebUI default-src blocks it anyway. It is live in the Vite dev server. DORMANT, not DISABLE_RUNTIME: nobody decided to switch it off — the code was written and the surrounding arrangement happens to stop it, which is exactly the state that becomes reachable again when someone relaxes a CSP for an unrelated reason. |
| `www.9oo91e.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `www.amazon.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `www.ch40m1um.qjz9zk` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A substituted hostname carried as literal text by an Astro branding patch — a translator `desc=` note, an `<ex>` placeholder or a C++ comment. Never rendered as a URL and never fetched. Domain substitution has never run here, so this shape is text somebody committed, not an endpoint the pipeline mangled. Recorded rather than ignored because the shape is embarrassing in shipped translator notes and #9 should clear it. |
| `www.chromium.org` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | Chromium project documentation links in comments and package metadata. |
| `www.espn.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `www.example-url.com` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | A placeholder in Chromium's branding strings. |
| `www.googleapis.com` | RUNTIME_BLOCKED | **DISABLE_BUILD** | blocklist-literal | `google-account-gaia` | #8 | the owning feature's own trigger, if it runs | The generic Google APIs host, referenced from 51 sites across the tree. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `www.gstatic.com` | RUNTIME_BLOCKED | **INVESTIGATE** | blocklist-literal | `google-static-assets` | #10 | the owning feature's own trigger, if it runs | A Google static-asset host, also the usual captive-portal probe origin. The inherited iridium patch `all-add-trk-prefixes-to-possibly-evil-connections.patch` rewrites the URL literal to `trk:NNN:https://…`, and `block-trk-and-subdomains.patch` makes `trk:` unresolvable (components/url_formatter/url_fixer.cc returns an empty GURL). So the host appears on BOTH diff sides: the literal survives, the request cannot. That is DISABLE_RUNTIME as an observed state — the calling code is still compiled and reachable — which is why it may never be asserted as 'cannot happen'. |
| `www.instagram.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `www.nytimes.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |
| `www.w3.org` | NOT_AN_ENDPOINT | **DORMANT** | documentation | `none` | #10 | none | The SVG XML namespace URI, plus specification links in CSS and settings copy. A namespace is an identifier, not an address: nothing fetches it. |
| `www.yahoo.com` | REMOVED_BY_PATCH | **DISABLE_BUILD** | deleted-by-patch | `ntp-tiles` | #22 | none | Part of Chromium's prepopulated New Tab Page data, deleted by patches/astro/019-ntp-default-sites.patch. Present in this manifest because the host appears in patch TEXT; it appears only on removed lines, so the patched tree does not contain it. Several of these were favicon URLs the NTP fetched on render, which is why removing them is a network change and not only a cosmetic one. DISABLE_BUILD: the string is absent from the shipped resource, which a `strings` scan can falsify. |

## Seed provenance

| Source | Hosts | What it reads |
|---|---|---|
| `baseline-inventory` | 94 | `src/**/*.{cc,h,mojom,rs}` and `patches/**/*.patch`, via `tools/baseline/inventory_endpoints.py` |
| `webui-scan` | 18 | `webui/*/src/**` — not read by the baseline generator |
| both | 7 | referenced from each |
| `declared` | 3 | no committed reference; each entry says why it cannot be derived |

## Open questions

| Host | Owner | Question | Answered by |
|---|---|---|---|
| `api.alia.oxy.so` | #17 | Is api.alia.oxy.so a planned alias for api.alia.onl, or a mistake in the CSP? As committed, Alia's panel cannot reach its API: the CSP allows a host nothing calls and blocks the one it does. | Ask the Alia service owner which host is canonical, then fix whichever of astro_alia_ui.cc:95 or alia-panel.ts:31 is wrong. Measured input: the Alia repository references api.alia.onl 47 times and api.alia.oxy.so 0. |
| `astrobrowser.org` | #9 | Does Oxy own astrobrowser.org? A product help link to a domain the project does not control is a hijack waiting to happen, and #9 generates product constants from one source of truth — this one is hard-coded in frontend source instead. | Confirm registration with the domain owner; then either record it as an Astro product constant in #9's single source or replace the link. |
| `chrome.google.com` | #20 | Legacy Web Store host. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `chromewebstore.google.com` | #20 | The Chrome Web Store. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `chromewebstore.googleapis.com` | #20 | The Web Store's API. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `clients2.google.com` | #20 | Omaha/component-update and crash endpoints. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `cloudflare-dns.com` | #20 | Does Astro ship a DoH provider ON by default, and if so which? The settings UI offers Cloudflare as the default value, but whether `dns_over_https.mode` is `secure`/`automatic` out of the box is not established, and the difference is between 'no default DNS provider' and 'every lookup goes to Cloudflare'. | Read the registered default for `dns_over_https.mode` in the built profile, then decide as part of #20's transport-security baseline. Astro registers no default for it in the committed patch stack — 020-register-oxy-prefs.patch does not appear in docs/astro-next/baseline/pref-dispositions.json for these keys. |
| `connectivitycheck.gstatic.com` | #10 | Does Astro contact connectivitycheck.gstatic.com, on what trigger, and with what payload? The source scan cannot answer it: the inventory does not read the Chromium tree, and no Astro patch touches this host. | A measured cold-start and idle trace from tools/baseline/capture-network.sh, which is blocked until an Astro binary exists (baseline finding 1). Until then this stays INVESTIGATE and no claim about Astro's network behaviour may be made without it. |
| `dns.nextdns.io` | #20 | Does Astro ship a DoH provider ON by default, and if so which? The settings UI offers Cloudflare as the default value, but whether `dns_over_https.mode` is `secure`/`automatic` out of the box is not established, and the difference is between 'no default DNS provider' and 'every lookup goes to Cloudflare'. | Read the registered default for `dns_over_https.mode` in the built profile, then decide as part of #20's transport-security baseline. Astro registers no default for it in the committed patch stack — 020-register-oxy-prefs.patch does not appear in docs/astro-next/baseline/pref-dispositions.json for these keys. |
| `dns.quad9.net` | #20 | Does Astro ship a DoH provider ON by default, and if so which? The settings UI offers Cloudflare as the default value, but whether `dns_over_https.mode` is `secure`/`automatic` out of the box is not established, and the difference is between 'no default DNS provider' and 'every lookup goes to Cloudflare'. | Read the registered default for `dns_over_https.mode` in the built profile, then decide as part of #20's transport-security baseline. Astro registers no default for it in the committed patch stack — 020-register-oxy-prefs.patch does not appear in docs/astro-next/baseline/pref-dispositions.json for these keys. |
| `optimizationguide-pa.googleapis.com` | #8 | Does Astro contact optimizationguide-pa.googleapis.com, on what trigger, and with what payload? The source scan cannot answer it: the inventory does not read the Chromium tree, and no Astro patch touches this host. | A measured cold-start and idle trace from tools/baseline/capture-network.sh, which is blocked until an Astro binary exists (baseline finding 1). Until then this stays INVESTIGATE and no claim about Astro's network behaviour may be made without it. |
| `redirector.gvt1.com` | #20 | The CRX/component download redirector. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `safebrowsing.google.com` | #20 | The Safe Browsing service. Whether Astro ships malicious-site and dangerous-download protection, and through whose backend, is #20's decision. Listed here for completeness and routed; deliberately not classified. | #20's malicious-site protection ADR. |
| `safebrowsingohttpgateway.googleapis.com` | #20 | Safe Browsing's OHTTP gateway. Whether Astro ships malicious-site protection, and through which provider, is #20's decision. This entry records the host and routes it; it does not classify it. | #20's malicious-site protection ADR. |
| `sb-ssl.google.com` | #20 | A Safe Browsing endpoint. Whether Astro ships malicious-site and dangerous-download protection, and through whose backend, is #20's decision. Listed here for completeness and routed; deliberately not classified. | #20's malicious-site protection ADR. |
| `ssl.gstatic.com` | #10 | A Google static-asset host. Which upstream features fetch it — captive-portal probe, interstitial images, translate assets? A host-level disposition is meaningless until the feature list behind it is enumerated. | git grep -l 'gstatic.com' in chromium/src at the locked commit, grouped by owning component. |
| `tools.google.com` | #20 | The update ping host; also targeted by block-requests.patch. Extensions and components that never update are a security problem, and the epic forbids disabling a protection merely to remove a provider dependency. So this is not a privacy decision to take here: it is update integrity, which is #20's. | #20's update-integrity design, then #25 for the transport that delivers it. |
| `translate.googleapis.com` | #10 | The translation backend. Is translation a capability Astro ships? If yes it needs a provider, and the epic forbids removing a capability merely because its code mentions Google. If no, the UI entry points go with it. | A product decision on this issue, then a provider contract in #28. |
| `transparencyreport.google.com` | #20 | Referenced by safe_browsing-disable-incident-reporting.patch. Whether Astro ships malicious-site and dangerous-download protection, and through whose backend, is #20's decision. Listed here for completeness and routed; deliberately not classified. | #20's malicious-site protection ADR. |
| `update.googleapis.com` | #20 | Does Astro contact update.googleapis.com, on what trigger, and with what payload? The source scan cannot answer it: the inventory does not read the Chromium tree, and no Astro patch touches this host. | A measured cold-start and idle trace from tools/baseline/capture-network.sh, which is blocked until an Astro binary exists (baseline finding 1). Until then this stays INVESTIGATE and no claim about Astro's network behaviour may be made without it. |
| `www.gstatic.com` | #10 | A Google static-asset host, also the usual captive-portal probe origin. Which upstream features fetch it — captive-portal probe, interstitial images, translate assets? A host-level disposition is meaningless until the feature list behind it is enumerated. | git grep -l 'gstatic.com' in chromium/src at the locked commit, grouped by owning component. |

