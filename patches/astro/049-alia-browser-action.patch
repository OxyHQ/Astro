--- a/chrome/browser/ui/browser_actions.cc
+++ b/chrome/browser/ui/browser_actions.cc
@@ -262,6 +262,20 @@
                               kActionSidePanelShowMerchantTrust, bwi, false))
           .Build());
 
+  // Oxy: Register Alia AI side panel action with spark icon.
+  root_action_item_->AddChild(
+      actions::ActionItem::Builder(
+          CreateToggleSidePanelActionCallback(
+              SidePanelEntryKey(SidePanelEntryId::kAlia), bwi))
+          .SetActionId(kActionSidePanelShowAlia)
+          .SetText(u"Alia")
+          .SetTooltipText(u"Alia AI Assistant")
+          .SetImage(ui::ImageModel::FromVectorIcon(kAliaSparkIcon, ui::kColorIcon))
+          .SetProperty(actions::kActionItemPinnableKey,
+                       std::underlying_type_t<actions::ActionPinnableState>(
+                           actions::ActionPinnableState::kPinnable))
+          .Build());
+
   if (side_panel::history_clusters::
           IsHistoryClustersSidePanelSupportedForProfile(profile) &&
       !HistorySidePanelCoordinator::IsSupported()) {
