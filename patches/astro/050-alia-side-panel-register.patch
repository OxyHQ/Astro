diff --git a/chrome/browser/ui/views/side_panel/side_panel_util.cc b/chrome/browser/ui/views/side_panel/side_panel_util.cc
--- a/chrome/browser/ui/views/side_panel/side_panel_util.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel_util.cc
@@ -37,6 +37,9 @@
 #include "ui/accessibility/accessibility_features.h"
 #include "ui/actions/actions.h"

+// Oxy: Alia AI side panel
+#include "chrome/browser/oxy/oxy_alia_side_panel.h"
+
 #if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/glic/public/glic_enabling.h"
 #include "chrome/browser/ui/views/side_panel/glic/glic_legacy_side_panel_coordinator.h"
@@ -122,6 +125,13 @@
   }
 #endif
+
+  // Oxy: Add Alia AI side panel.
+  if (browser->is_type_normal()) {
+    auto alia_coordinator =
+        std::make_unique<oxy::AliaSidePanelCoordinator>(browser);
+    alia_coordinator->CreateAndRegisterEntry(window_registry);
+    // Note: the coordinator is intentionally not stored — the entry owns
+    // the creation callback via base::Unretained, which is safe because
+    // the entry (and thus the callback) is destroyed when the registry
+    // is destroyed, which happens before the browser is destroyed.
+  }
 }

 SidePanelContentProxy* SidePanelUtil::GetSidePanelContentProxy(
